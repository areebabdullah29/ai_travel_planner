"""
Destination tools — stub kept for import compatibility.

The destination_agent now uses google_search directly to find live destination
data (attractions, restaurants, events, costs) for any city in the world.
These helper functions are no longer used and can be safely removed in a future cleanup.
"""
